import {PageShell} from "./site-shell";
export function LegalPage({eyebrow,title,intro,updated="4 September 2026",sections}:{eyebrow:string;title:string;intro:string;updated?:string;sections:{title:string;body:string}[]}){return <PageShell><article className="legal-page section"><header><span>{eyebrow}</span><h1>{title}</h1><p>{intro}</p><small>Last updated: {updated}</small></header><nav aria-label="On this page"><b>On this page</b>{sections.map((s,i)=><a href={`#section-${i}`} key={s.title}>{s.title}</a>)}</nav><div>{sections.map((s,i)=><section id={`section-${i}`} key={s.title}><h2>{s.title}</h2>{s.body.split("\n").map((p,j)=><p key={j}>{p}</p>)}</section>)}</div></article></PageShell>}
