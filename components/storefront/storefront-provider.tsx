"use client";
import {createContext,useContext,useEffect,useMemo,useState} from "react";
type CartItem={productId:string;quantity:number};type State={cart:CartItem[];wishlist:string[];addToCart:(id:string,q?:number)=>void;setQuantity:(id:string,q:number)=>void;removeFromCart:(id:string)=>void;clearCart:()=>void;toggleWishlist:(id:string)=>void;cartCount:number};const C=createContext<State|null>(null);
export function StorefrontProvider({children}:{children:React.ReactNode}){const [cart,setCart]=useState<CartItem[]>([]),[wishlist,setWishlist]=useState<string[]>([]),[ready,setReady]=useState(false);useEffect(()=>{try{setCart(JSON.parse(localStorage.getItem("1clickmela-cart")||"[]"));setWishlist(JSON.parse(localStorage.getItem("1clickmela-wishlist")||"[]"))}catch{}setReady(true)},[]);useEffect(()=>{if(ready)localStorage.setItem("1clickmela-cart",JSON.stringify(cart))},[cart,ready]);useEffect(()=>{if(ready)localStorage.setItem("1clickmela-wishlist",JSON.stringify(wishlist))},[wishlist,ready]);const value=useMemo(()=>({cart,wishlist,cartCount:cart.reduce((n,x)=>n+x.quantity,0),addToCart:(id:string,q=1)=>setCart(c=>{const x=c.find(i=>i.productId===id);return x?c.map(i=>i.productId===id?{...i,quantity:i.quantity+q}:i):[...c,{productId:id,quantity:q}]}),setQuantity:(id:string,q:number)=>setCart(c=>q<1?c.filter(i=>i.productId!==id):c.map(i=>i.productId===id?{...i,quantity:q}:i)),removeFromCart:(id:string)=>setCart(c=>c.filter(i=>i.productId!==id)),clearCart:()=>setCart([]),toggleWishlist:(id:string)=>setWishlist(w=>w.includes(id)?w.filter(x=>x!==id):[...w,id])}),[cart,wishlist]);return <C.Provider value={value}>{children}</C.Provider>};export const useStorefront=()=>{const c=useContext(C);if(!c)throw new Error("StorefrontProvider missing");return c};
