"use client";import {FormEvent,useState} from "react";import {CheckCircle2,MapPin,PackageCheck,Truck} from "lucide-react";import {PageShell} from "@/components/storefront/site-shell";
export default function TrackOrder(){const [id,setId]=useState("");const [email,setEmail]=useState("");const [checked,setChecked]=useState(false);function submit(e:FormEvent){e.preventDefault();setChecked(true)}return <PageShell><section className="utility-page section"><div className="utility-intro"><span>ORDER SUPPORT</span><h1>Track your order</h1><p>Enter the order number from your confirmation and the email or phone number used at checkout.</p></div><form className="track-card" onSubmit={submit}><label>Order number<input required value={id} onChange={e=>{setId(e.target.value);setChecked(false)}} placeholder="e.g. 1CM-10248"/></label><label>Email or phone<input required value={email} onChange={e=>{setEmail(e.target.value);setChecked(false)}} placeholder="Your contact details"/></label><button>Track order</button>{checked&&<div className="track-result"><PackageCheck/><div><b>We couldn't find that order yet</b><p>Please check the order number and contact details. New orders may take a few minutes to appear.</p></div></div>}</form><div className="order-steps"><div><CheckCircle2/><b>Order confirmed</b></div><div><PackageCheck/><b>Packed</b></div><div><Truck/><b>On the way</b></div><div><MapPin/><b>Delivered</b></div></div></section></PageShell>}
