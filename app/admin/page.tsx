"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";import {supabase} from "@/lib/supabase";import AdminDashboard from "./admin-client";
export default function Page(){const router=useRouter(),[ready,setReady]=useState(false),[name,setName]=useState("Admin");useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user){router.replace("/admin/login");return}const {data}=await supabase.rpc("is_admin");if(!data){await supabase.auth.signOut();router.replace("/admin/login?error=unauthorised");return}setName(String(user.user_metadata?.full_name||user.email||"Admin"));setReady(true)})()},[router]);if(!ready)return <main className="admin-auth-loading"><span/><p>Verifying secure admin access…</p></main>;return <AdminDashboard adminName={name} signOutPath="/admin/login" supabaseMode/>}
