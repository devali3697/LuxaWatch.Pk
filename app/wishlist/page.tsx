"use client";
import Link from "next/link";import {Heart} from "lucide-react";import {store} from "@/lib/storefront-data";import {PageShell} from "@/components/storefront/site-shell";import {Catalog} from "@/components/storefront/catalog";import {useStorefront} from "@/components/storefront/storefront-provider";
export default function Page(){const {wishlist}=useStorefront();const products=store.products.filter(p=>wishlist.includes(p.id));return <PageShell><section className="page-head section"><span>SAVED FOR LATER</span><h1>Your wishlist</h1><p>{products.length?`${products.length} product${products.length>1?"s":""} you love`:"Keep all your favourites in one place."}</p></section><section className="section search-results">{products.length?<Catalog products={products}/>:<div className="empty"><Heart/><h2>Your wishlist is empty</h2><p>Tap the heart on any product to save it here.</p><Link href="/">Start shopping</Link></div>}</section></PageShell>}
