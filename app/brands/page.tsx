import Link from "next/link";import {ArrowRight} from "lucide-react";import {PageShell} from "@/components/storefront/site-shell";import {store} from "@/lib/storefront-data";
export default function BrandsPage(){return <PageShell><section className="page-head section"><span>TRUSTED NAMES</span><h1>Shop by Brand</h1><p>Explore genuine products from brands customers know and trust.</p></section><section className="brand-directory section">{store.brands.map(b=><Link href={`/search?q=${encodeURIComponent(b.name)}`} key={b.name}><div><img src={b.logo} alt={`${b.name} logo`}/></div><h2>{b.name}</h2><p>{b.tagline}</p><span>Shop brand <ArrowRight/></span></Link>)}</section></PageShell>}
